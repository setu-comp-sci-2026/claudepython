# Challenges

## Challenge 1 — Recursive Sum

Write a recursive function `recursive_sum(numbers)` that adds up all numbers in a list without using `sum()` or a for loop.

```python
def recursive_sum(numbers):
    # Base case: empty list returns 0
    # Recursive case: first + recursive_sum(rest)
    pass

print(recursive_sum([1, 2, 3, 4, 5]))  # 15
print(recursive_sum([]))                # 0
```

## Challenge 2 — Caesar Cipher

The Caesar cipher shifts each letter by a fixed number of positions.

```python
def encrypt(text, shift):
    result = ""
    for char in text:
        if char.isalpha():
            base = ord('a') if char.islower() else ord('A')
            shifted = (ord(char) - base + shift) % 26 + base
            result += chr(shifted)
        else:
            result += char
    return result

def decrypt(text, shift):
    return encrypt(text, -shift)
```

Use these functions to:
1. Encrypt a message of your choice with shift=13
2. Decrypt it back
3. Try to crack an unknown ciphertext by trying all 26 shifts

## Challenge 3 — Word Frequency Counter

Write a function `word_frequency(text)` that returns a list of `(word, count)` tuples sorted by frequency (highest first), then alphabetically for ties.

```python
text = "the quick brown fox jumps over the lazy dog the fox"
for word, count in word_frequency(text):
    print(f"{word:<12} {count}")
```

Expected: "the" (3), "fox" (2), then all single-occurrence words alphabetically.
